import React from "react";
import ProfileContent from "@/components/profile/ProfileContent";

const EmployerProfile: React.FC = () => {
    return <ProfileContent forcedAccountType="employer" />;
};

export default EmployerProfile;
